# TODO - Apartado de búsqueda (buscar por nombre completo)

- [ ] Actualizar `ProductoRepository` con método `findByNombreIgnoreCase`.
- [ ] Actualizar `ProductoService` con método `listarPorNombre`.
- [ ] Actualizar `ProductoController` para aceptar `nombre` como parámetro opcional en `GET /`.
- [ ] Actualizar `index.html` con formulario GET de búsqueda y preservar el valor del input.
- [ ] Ejecutar `mvn test` o `mvn spring-boot:run` para validar el flujo.

